-- module RPWeb.Msgs exposing (..)

-- import RPWeb.Models exposing (..)
-- import RemoteData exposing (WebData)
-- --import RPWeb.RollMsgs  exposing (RollMsg)

-- type Msg
--     = 
--         --OnFetchActionRequiredROs (WebData (List RO ))
--     |   --OnFetchInProcessROs (WebData (List RPWebModels.InProcessRO))
--     |   Refresh Int
--     |   Roll RollMsg

-- type RollMsg
--     = ActionRequiredROsMsg
--     | InProcessROsMsg
